DROP TABLE IF EXISTS badania;

CREATE TABLE badania(
	IDbadania INT UNSIGNED PRIMARY KEY,
	#	FOREIGN KEY REFERENCES wizyty(IDbadania),
	typBadania VARCHAR(64),
	dataBadania DATE

	#FOREIGN KEY fk_IDbadania(IDbadania)
	#REFERENCES wizyty(IDbadania)
);
